params {
    inputDir    = "${projectDir}/GSM1/"
    resultDir   = "${projectDir}/results"
    scriptFile  = "${projectDir}/r-scripts"
    analysis   = 'scanpy'   // or 'seurat'
    harmony = "yes"  // "yes" or "no"  to run harmonization through hrmony algorithm
}
